
當您在虛擬化平台 (如 Proxmox VE) 上增加了虛擬硬碟的大小後，Ubuntu 虛擬機本身並不會立即識別並使用這些新的空間。您需要透過以下步驟，讓 Ubuntu 系統將這些新增的空間整合進現有的檔案系統中。

整個過程的核心步驟可以分為兩大階段：

階段一：在虛擬化平台 (PVE) 上擴展虛擬磁碟
關閉虛擬機 (推薦)： 雖然某些虛擬化平台支援熱擴展，但為了資料安全和避免不必要的錯誤，強烈建議先將 Ubuntu 虛擬機關機。

修改虛擬硬碟大小：

登入您的 Proxmox VE 管理介面。

選擇您要擴展的 Ubuntu 虛擬機。

進入該虛擬機的「硬體」設定。

找到您要擴展的硬碟 (例如 scsi0 或 virtio0)，選擇它。

點擊「編輯」，然後在「磁碟大小」欄位中輸入您想要的新總大小 (例如 64G)。

儲存設定。

啟動虛擬機： 完成 PVE 上的設定後，啟動您的 Ubuntu 虛擬機。

階段二：在 Ubuntu 虛擬機內部擴展檔案系統
此階段所有命令都在 Ubuntu 虛擬機的終端機中執行。

確認磁碟佈局：

使用 lsblk 命令查看當前磁碟和分區的佈局，確認新增的空間已經被虛擬機識別，以及您的根分區 (通常是 LVM 的底層分區) 是哪個。

命令： lsblk

目的： 找到您的主硬碟 (例如 sda)，以及其下的 LVM 所在分區 (例如 sda3)。

擴展 LVM 的底層分區 (growpart)：

由於 Ubuntu 伺服器版通常會使用 LVM，您需要先擴展 LVM 所在的實體分區，讓它佔用整個虛擬硬碟上新增加的空間。

命令： sudo growpart /dev/sda 3 (請將 sda 和 3 替換為您實際的硬碟和分區號碼)

目的： 將指定分區 (例如 sda3) 擴展到其後面的所有可用空間。

擴展 LVM 實體卷 (pvresize)：

告訴 LVM 系統，它底層的實體卷現在有了更多的空間可以使用。

命令： sudo pvresize /dev/sda3 (請將 sda3 替換為您實際的實體卷所在分區)

目的： 讓 LVM 認識到實體卷的容量已經增加。

擴展 LVM 邏輯卷 (lvextend)：

將您的根目錄所使用的邏輯卷 (/dev/mapper/ubuntu--vg-ubuntu--lv 或 /dev/ubuntu-vg/ubuntu-lv) 擴展到卷組中所有可用的空間。

命令： sudo lvextend -l +100%FREE /dev/mapper/ubuntu--vg-ubuntu--lv

目的： 讓邏輯卷佔用更多空間，為檔案系統擴展做準備。

擴展檔案系統 (resize2fs)：

最後一步是擴展實際的檔案系統，使其能夠使用邏輯卷上所有新增的空間。

命令： sudo resize2fs /dev/mapper/ubuntu--vg-ubuntu--lv

目的： 將檔案系統的大小調整到與邏輯卷相同，使其真正可以使用新增的空間。

驗證：

運行 df -h 命令，檢查您的根目錄 (/) 的「容量 (Size)」是否已成功擴展到預期的總大小。

命令： df -h

目的： 確認擴展操作成功。

重點提醒：

在執行任何磁碟操作前，備份重要資料是最佳實踐。

務必根據您實際的 lsblk 輸出，替換命令中的磁碟和分區名稱，例如 /dev/sda 和 3。

按照這些步驟操作，您就能成功擴展 Ubuntu 虛擬機的硬碟空間了！